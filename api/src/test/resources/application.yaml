spring:
  security:
    jwt:
      key: MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=
      cookie:
        name: authToken
        expires-in: 900
  datasource:
    url: jdbc:h2:mem:api_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  mail:
    host: localhost
    port: 2525
    username:
    password:
  minio:
    endpoint: http://localhost:9000
    public-endpoint: http://localhost:9000
    access_key: test
    secret_key: test
    listing_bucket: listings
    user_bucket: users
